```bash
kubectl describe cm kubeadm-config -n kube-system

kubectl describe cm kubeadm-config -n kube-system | grep podSubnet

k get node controlplane -oyaml | grep -i cidr
k get node node01 -oyaml | grep -i cidr
```