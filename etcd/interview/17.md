### etcd Failure Scenario

**What happens when etcd leader crashes in a 3-node cluster:**
- Remaining nodes elect a new leader using RAFT consensus
- Requires quorum (2/3 nodes)
- If only 1 node remains, cluster becomes read-only

**Recovery:**
```bash
etcdctl snapshot restore snapshot.db
```