```bash
echo "IP_ADDRESS"; kubectl get po -l app=nginx-app -ojsonpath='{range .items[*]}{.status.podIP}{"\n"}{end}' | sort 

```
```bash
( echo "IP_ADDRESS"; kubectl get po -l app=nginx-app -ojsonpath='{range .items[*]}{.status.podIP}{"\n"}{end}' | sort ) > pod_ips.txt

{ echo "IP_ADDRESS"; kubectl get po -l app=nginx-app -ojsonpath='{range .items[*]}{.status.podIP}{"\n"}{end}' | sort; } | tee pod_ips.txt

{ echo "IP_ADDRESS"; kubectl get po -l app=nginx-app -ojsonpath='{range .items[*]}{.status.podIP}{"\n"}{end}' | sort; } > pod_ips.txt
```