```bash
k8s/
├── db/
│   ├── db-config.yaml
│   ├── db-deploy.yaml
│   └── db-service.yaml
├── message-broker/
│   ├── rabbitmq-config.yaml
│   ├── rabbitmq-deploy.yaml
│   └── rabbitmq-service.yaml
├── nginx/
│   ├── nginx-deploy.yaml
│   └── nginx-service.yaml
└── kustomization.yaml
```

---

## Create Folder Structure
```bash
mkdir -p k8s/db
mkdir -p k8s/message-broker
mkdir -p k8s/nginx
```

---

## DB (MySQL)

#### k8s/db/db-config.yaml
```bash
cat <<EOF > k8s/db/db-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
data:
  MYSQL_DATABASE: appdb
EOF
```

#### k8s/db/db-deploy.yaml
```bash
cat <<EOF > k8s/db/db-deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: rootpassword
        - name: MYSQL_DATABASE
          valueFrom:
            configMapKeyRef:
              name: mysql-config
              key: MYSQL_DATABASE
EOF
```

#### k8s/db/db-service.yaml
```bash
cat <<EOF > k8s/db/db-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
  clusterIP: None
EOF
```

---

## Message Broker (RabbitMQ)

#### k8s/message-broker/rabbitmq-config.yaml
```bash
cat <<EOF > k8s/message-broker/rabbitmq-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: rabbitmq-config
data:
  RABBITMQ_DEFAULT_USER: guest
EOF
```

#### k8s/message-broker/rabbitmq-deploy.yaml
```bash
cat <<EOF > k8s/message-broker/rabbitmq-deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rabbitmq
  template:
    metadata:
      labels:
        app: rabbitmq
    spec:
      containers:
      - name: rabbitmq
        image: rabbitmq:3-management
        ports:
        - containerPort: 5672
        - containerPort: 15672
EOF
```

#### k8s/message-broker/rabbitmq-service.yaml
```bash
cat <<EOF > k8s/message-broker/rabbitmq-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
spec:
  selector:
    app: rabbitmq
  ports:
    - port: 5672
    - port: 15672
  type: ClusterIP
EOF
```

---

## Nginx

#### k8s/nginx/nginx-deploy.yaml

```bash
cat <<EOF > k8s/nginx/nginx-deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
EOF
```

#### k8s/nginx/nginx-service.yaml
```bash
cat <<EOF > k8s/nginx/nginx-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    app: nginx
  ports:
    - port: 80
  type: NodePort
EOF
```

---

## Root kustomization.yaml
This ties everything together.

#### 📄 k8s/kustomization.yaml
```bash
cat <<EOF > k8s/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - db/db-config.yaml
  - db/db-deploy.yaml
  - db/db-service.yaml
  - message-broker/rabbitmq-config.yaml
  - message-broker/rabbitmq-deploy.yaml
  - message-broker/rabbitmq-service.yaml
  - nginx/nginx-deploy.yaml
  - nginx/nginx-service.yaml

labels:
  - pairs:
        project: microservice-demo
    includeSelectors: true
EOF
```


#### Deploy Everything
From inside `k8s` directory:
```bash
kubectl apply -k .
```

#### Verify
```bash
kubectl get pods
kubectl get svc
kubectl get deploy
kubectl get all
```

