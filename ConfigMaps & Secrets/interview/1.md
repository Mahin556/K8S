### A Node.js application requires environment variables stored in a ConfigMap named node-environment-config. Inject these variables into the pod.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: nodejs-pod
spec:
  containers:
  - name: app
    image: node:18
    envFrom:
    - configMapRef:
        name: node-env-config
```