#### How Worker Nodes Connect?
- Worker nodes don't connect to a specific master node
- A **load balancer** sits in front of all 3 master nodes
- Worker nodes connect to the **load balancer's IP**
- Load balancer forwards to available apiserver
- Write queries → forwarded to leader
- Read queries → can be served by any member