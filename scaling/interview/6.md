## What resource is used for auto-scaling pods based on CPU/memory?

**Answer:** **HPA (Horizontal Pod Autoscaler)**

- **When it triggers:** When CPU/memory usage exceeds threshold (e.g., 75%)
- **What it does:** Automatically increases/decreases replica count
- **Configuration:** Min replicas, max replicas, target CPU/memory percentage
